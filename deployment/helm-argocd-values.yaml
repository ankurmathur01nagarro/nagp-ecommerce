# ArgoCD Helm values override
# Restores the Application health check that was removed in ArgoCD v1.8.
# Without this, sync waves in App-of-Apps won't wait for child apps to become Healthy.
# Ref: https://argo-cd.readthedocs.io/en/stable/operator-manual/health/#argocd-app
configs:
  cm:
    # Restore Application health assessment for sync-wave support
    resource.customizations.health.argoproj.io_Application: |
      hs = {}
      hs.status = "Progressing"
      hs.message = ""
      if obj.status ~= nil then
        if obj.status.health ~= nil then
          hs.status = obj.status.health.status
          if obj.status.health.message ~= nil then
            hs.message = obj.status.health.message
          end
        end
      end
      return hs
