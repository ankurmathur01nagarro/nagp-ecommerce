apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: web-api-secrets
  namespace: nagp-ecom
spec:
  secretStoreRef:
    name: secrets-store
    kind: ClusterSecretStore
  data:
    - secretKey: ECOMAPI_CLIENT_SECRET 
      remoteRef:
        key: secret/staging/identity-api
        property: ECOMAPI_CLIENT_SECRET
  target:
    name: web-api-secrets