apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: nagp-ecom
spec:
  template:
    spec:
      containers:
        - name: postgres
          resources:
            requests:
              cpu: 100m
              memory: 500Mi
            limits:
              cpu: 250m
              memory: 1Gi
