apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: identity-api-secrets
  namespace: nagp-ecom
spec:
  secretStoreRef:
    name: secrets-store
    kind: ClusterSecretStore
  data:
    - secretKey: ECOMAPI_CLIENT_ID
      remoteRef:
        key: secret/ECOMAPI-CLIENT-ID
    - secretKey: ECOMAPI_CLIENT_SECRET
      remoteRef:
        key: secret/ECOMAPI-CLIENT-SECRET
  target:
    name: identity-api-secrets