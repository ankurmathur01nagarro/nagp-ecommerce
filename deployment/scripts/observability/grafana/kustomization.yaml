apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: grafana

# Kustomize manages PVC only; Helm chart deployed separately by ArgoCD multi-source
# For environment-specific PVC config, use overlays:
#   - overlays/local   (local-path storage)
#   - overlays/prod  (standard cloud storage)
resources:
  - base
